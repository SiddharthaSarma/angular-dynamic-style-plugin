import { ApplyDynamicStylesPipe } from './apply-dynamic-styles.pipe';

describe('ApplyDynamicStylesPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyDynamicStylesPipe();
    expect(pipe).toBeTruthy();
  });
});
